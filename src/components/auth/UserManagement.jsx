import { Plus, UserRound } from 'lucide-react';
import { useState } from 'react';
import { ROLES } from '../../utils/permissions';

const emptyUser = { name: '', email: '', password: '', role: ROLES.STAFF };
export const UserManagement = ({ users, onAdd, onUpdate }) => {
  const [form, setForm] = useState(emptyUser); const [message, setMessage] = useState('');
  const add = (event) => { event.preventDefault(); try { if (!form.name.trim() || !form.email.trim() || form.password.length < 8) throw new Error('Name, email, and a password of at least 8 characters are required.'); onAdd(form); setForm(emptyUser); setMessage('User added successfully.'); } catch (error) { setMessage(error.message); } };
  return <div className="space-y-6"><section className="rounded-2xl border border-slate-200/70 bg-white p-5 shadow-xs dark:border-slate-800 dark:bg-slate-900"><div className="mb-5 flex items-center gap-3"><div className="rounded-xl bg-blue-50 p-2.5 text-blue-600 dark:bg-blue-950/50"><UserRound className="h-5 w-5" /></div><div><h2 className="text-lg font-bold text-slate-900 dark:text-white">User management</h2><p className="text-sm text-slate-500 dark:text-slate-400">Manage local frontend accounts and role access.</p></div></div><form onSubmit={add} className="grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-4"><input value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} placeholder="Name" className="rounded-xl border border-slate-200 bg-slate-50 px-3 py-2.5 text-sm dark:border-slate-700 dark:bg-slate-800" /><input value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} placeholder="Email" type="email" className="rounded-xl border border-slate-200 bg-slate-50 px-3 py-2.5 text-sm dark:border-slate-700 dark:bg-slate-800" /><input value={form.password} onChange={(event) => setForm({ ...form, password: event.target.value })} placeholder="Temporary password" type="password" className="rounded-xl border border-slate-200 bg-slate-50 px-3 py-2.5 text-sm dark:border-slate-700 dark:bg-slate-800" /><div className="flex gap-2"><select value={form.role} onChange={(event) => setForm({ ...form, role: event.target.value })} className="min-w-0 flex-1 rounded-xl border border-slate-200 bg-slate-50 px-3 py-2.5 text-sm dark:border-slate-700 dark:bg-slate-800">{Object.values(ROLES).map((role) => <option key={role}>{role}</option>)}</select><button className="inline-flex items-center gap-1 rounded-xl bg-blue-600 px-3 py-2.5 text-sm font-semibold text-white"><Plus className="h-4 w-4" />Add</button></div></form>{message && <p role="status" className="mt-3 text-sm text-slate-600 dark:text-slate-300">{message}</p>}</section><section className="overflow-hidden rounded-2xl border border-slate-200/70 bg-white shadow-xs dark:border-slate-800 dark:bg-slate-900"><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-sm"><thead><tr className="border-b border-slate-100 dark:border-slate-800"><th className="p-4 text-left">User</th><th className="p-4 text-left">Role</th><th className="p-4 text-left">Status</th><th className="p-4 text-right">Actions</th></tr></thead><tbody>{users.map((user) => <tr key={user.id} className="border-b border-slate-50 last:border-0 dark:border-slate-800"><td className="p-4"><div className="font-semibold text-slate-900 dark:text-white">{user.name}</div><div className="text-xs text-slate-500">{user.email}</div></td><td className="p-4"><select value={user.role} onChange={(event) => onUpdate(user.id, { role: event.target.value })} className="rounded-lg border border-slate-200 bg-slate-50 px-2 py-1.5 text-sm dark:border-slate-700 dark:bg-slate-800">{Object.values(ROLES).map((role) => <option key={role}>{role}</option>)}</select></td><td className="p-4"><span className={`rounded-md px-2 py-1 text-xs font-semibold ${user.isActive ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-600'}`}>{user.isActive ? 'Active' : 'Inactive'}</span></td><td className="p-4 text-right"><button type="button" onClick={() => onUpdate(user.id, { isActive: !user.isActive })} className="rounded-lg px-2.5 py-1.5 text-xs font-semibold text-blue-600 hover:bg-blue-50">{user.isActive ? 'Deactivate' : 'Activate'}</button></td></tr>)}</tbody></table></div></section></div>;
};
