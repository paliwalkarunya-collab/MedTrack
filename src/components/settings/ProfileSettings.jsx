import { ImagePlus, Trash2 } from 'lucide-react';
import { Field, inputClass, Section } from './SettingsControls';

const profileFields = [['businessName', 'Pharmacy / business name'], ['ownerName', 'Owner name'], ['businessType', 'Business type'], ['phone', 'Phone'], ['alternatePhone', 'Alternate phone'], ['email', 'Email'], ['website', 'Website'], ['address', 'Address'], ['city', 'City'], ['state', 'State'], ['postalCode', 'PIN / ZIP'], ['country', 'Country']];
const taxFields = [['gstin', 'GSTIN'], ['drugLicenseNumber', 'Drug license number'], ['pan', 'PAN'], ['stateGst', 'State GST details'], ['referenceNumber', 'Other license / reference']];

export const ProfileSettings = ({ settings, onChange, error, onLogoChange }) => <div className="space-y-6"><Section title="Business / pharmacy profile" description="These details are saved locally and can be used by future invoice presentation features."><div className="mb-5 flex flex-wrap items-center gap-4 rounded-xl bg-slate-50 p-4 dark:bg-slate-800/60">{settings.profile.logo ? <img src={settings.profile.logo} alt="Business logo preview" className="h-16 w-16 rounded-xl object-cover" /> : <div className="flex h-16 w-16 items-center justify-center rounded-xl bg-blue-50 text-blue-600 dark:bg-blue-950/50"><ImagePlus className="h-6 w-6" /></div>}<div className="flex flex-wrap gap-2"><label className="cursor-pointer rounded-xl bg-blue-600 px-3 py-2 text-sm font-semibold text-white hover:bg-blue-700"><input type="file" accept="image/png,image/jpeg,image/webp" className="sr-only" onChange={onLogoChange} />Upload logo</label>{settings.profile.logo && <button type="button" onClick={() => onChange('profile', 'logo', '')} className="inline-flex items-center gap-1.5 rounded-xl px-3 py-2 text-sm font-semibold text-rose-600 hover:bg-rose-50"><Trash2 className="h-4 w-4" />Remove</button>}<p className="basis-full text-xs text-slate-500">PNG, JPEG, or WebP up to 500 KB.</p>{error.logo && <p className="basis-full text-xs text-rose-600">{error.logo}</p>}</div></div><div className="grid grid-cols-1 gap-4 sm:grid-cols-2">{profileFields.map(([key, label]) => <Field key={key} label={label} error={error[key]}><input value={settings.profile[key]} type={key === 'email' ? 'email' : key === 'website' ? 'url' : 'text'} onChange={(event) => onChange('profile', key, event.target.value)} className={inputClass} /></Field>)}</div></Section><Section title="Tax & license information" description="Optional identifiers are stored exactly as entered, aside from leading and trailing spaces."><div className="grid grid-cols-1 gap-4 sm:grid-cols-2">{taxFields.map(([key, label]) => <Field key={key} label={label}><input value={settings.tax[key]} onChange={(event) => onChange('tax', key, event.target.value)} className={inputClass} /></Field>)}</div></Section></div>;
