import { Field, inputClass, Section, Toggle } from './SettingsControls';

export const PreferenceSettings = ({ settings, onChange, isDarkMode, setDarkMode, showNotifications = true, showAppearance = true }) => <div className="space-y-6">{showNotifications && <Section title="Notification preferences" description="These are saved preferences only; email and SMS delivery are not configured yet."><div className="grid grid-cols-1 gap-3 sm:grid-cols-2">{[['lowStock', 'Low-stock alerts'], ['expiry', 'Expiry alerts'], ['expired', 'Expired medicine alerts'], ['billing', 'Billing notifications'], ['system', 'System notifications']].map(([key, label]) => <Toggle key={key} checked={settings.notifications[key]} onChange={(value) => onChange('notifications', key, value)} label={label} />)}</div></Section>}{showAppearance && <Section title="Appearance" description="Visual preferences remain compatible with the current MedTrack layout."><div className="grid grid-cols-1 gap-4 sm:grid-cols-2"><Field label="Theme"><select value={isDarkMode ? 'dark' : 'light'} onChange={(event) => setDarkMode(event.target.value === 'dark')} className={inputClass}><option value="light">Light</option><option value="dark">Dark</option></select></Field><Field label="Content density"><select value={settings.appearance.density} onChange={(event) => onChange('appearance', 'density', event.target.value)} className={inputClass}><option value="comfortable">Comfortable</option><option value="compact">Compact</option></select></Field></div><div className="mt-4"><Toggle checked={settings.appearance.rememberSidebar} onChange={(value) => onChange('appearance', 'rememberSidebar', value)} label="Remember sidebar preference" description="Saved for future sidebar persistence support." /></div></Section>}</div>;
